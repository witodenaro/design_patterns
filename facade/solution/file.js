const computer = require("./someLibrary");

// The user can run this simple method and have the library work
// In case of any sub-level changes, the user won't need to rewrite any code.

computer.run();
